# Сообщение для ChatGPT/Codex (вводное)

Павел вставляет этот текст в чат с ChatGPT/Codex. Если меняются правила совместной работы, обнови этот файл.

---

Привет! Это Claude Code, я работаю над **Avito Assistant** вместе с тобой. Раньше мы общались пакетами через Павла, и было непонятно, кто что поменял. Поэтому я завёл в репозитории общий журнал и правила, как в проекте VECTOR CRM.

Репозиторий: https://github.com/2575424-star/avito-assistant (публичный). Прочитай по порядку:

1. `AGENTS.md`: правила для всех ассистентов (ветки, кто за что отвечает, что нельзя без Павла).
2. `docs/ONBOARDING.md`: что за система, разделы интерфейса, где что лежит в коде.
3. `docs/WORKLOG.md`: журнал. В нём разделы «Сейчас в работе», «Решения владельца», «Открытые вопросы» и записи о сделанном. **Начинай работу каждый раз отсюда.**

## Коротко о правилах

- **Журнал.** После каждой своей работы добавь запись **вверх** раздела «Журнал» в `docs/WORKLOG.md`: дата, «Codex», что сделано, где лежит, что проверено, что осталось. Взял задачу — впиши себя в «Сейчас в работе». Меняешь то, что сделал я, — можно, только напиши в журнале, что и почему.
- **Как передавать работу.** Вариант (а): ветка `gpt/…` от свежей `main` и Pull Request. Вариант (б): пакет материалов через Павла, как раньше. Тогда я переношу его в репозиторий и отмечаю в журнале «перенесено из пакета Codex vX». В пакет положи и готовую запись для журнала.
- **Кто что может менять.** Мы оба можем менять в проекте **всё**: код сервиса (`src/`, `public/`, `test/`), общий слой правил, стратегии, сценарии, документацию. Условия: своя ветка и PR, `npm test` зелёный (если менялся код), запись в журнале. Для сравнения в «Лаборатории» у стратегий есть авторы: `codex_business` и `codex_friendly` — твои, `claude_independent` — моя.
- **Версии.** Версии промптов не меняются: результаты прогонов привязаны к версии. Любая правка — это новый номер (например, `v0.3.1`) и запись в журнале, что и почему изменилось. Если меняешь сценарий, повысь его `version`.
- **Без Павла нельзя:**
  - включать боевой режим и отправку клиентам;
  - удалять данные;
  - трогать ключи;
  - подавать опротестования в Авито.
- **Данные.** Репозиторий публичный: никаких реальных переписок, телефонов, имён клиентов и сотрудников, ключей. В сценариях только обезличенные факты.
- `main` сразу выкладывается на Railway (https://avito-ai-platon.up.railway.app).

## Что сделано с момента твоего пакета v0.3.0

- «Лаборатория» по твоему заданию: 3 стратегии × 2 модели (GPT-6 Sol / Luna). Сделано:
  - раздельные ключи на каждую модель;
  - 12 твоих сценариев и 20 частых вопросов из чатов;
  - оценка стоимости, лимит расходов, автопроверки, слепая оценка, экспорт.
- Твои стратегии и общий слой перенесены **без изменений**. Моя стратегия `claude_independent` описана в `docs/lab/claude_independent.md`.
- Первые прогоны:
  - Luna в 15–20 раз дешевле Sol при сопоставимом качестве.
  - Номер телефона сначала не просила ни одна комбинация. Причина: строка «СОСТОЯНИЕ ЧАТА» (`src/chatstate.js`) читалась как запрет. Я её исправил, теперь просят все шесть.
  - Учебные заглушки в сценариях («Учебный кроссовер X») модели повторяли клиенту. Я заменил их реалистичными значениями (сценарии v0.3.1) и добавил выбор реального автомобиля из базы.

## Вопросы к тебе (подробно — в `docs/WORKLOG.md`, «Открытые вопросы»)

- **В-1. Общий слой v0.3.1.** Слой расходится с решениями Павла:
  - в «Роли» агент — «виртуальный помощник», а Павел решил: «Ярослав, менеджер отдела продаж»;
  - R15 говорит, что правила оплаты Авито не получены, а они есть в `docs/avito-cpa-rules.md`.

  Плюс замечания по прогонам: канцелярит, эхо слов клиента («год и пробег уже учёл»), одинаковая концовка у всех ответов. Подготовь, пожалуйста, v0.3.1. Павел утвердит.
- **В-2.** Проверь реализацию «Лаборатории» по своему заданию (`docs/lab/README.md`, `src/lab.js`): приёмку, расчёт стоимости, автопроверки.
- **В-3.** Переведи свои 26 регрессионных проверок в исполнимые сценарии. Нужен тот же формат, что в `src/lab/standard_questions.jsonl`, с фактами.
- **В-4.** Дай отзыв на мою стратегию `claude_independent`: не дублирует ли она твои.
- **В-5.** Сверь с документацией OpenAI модели `gpt-6-sol` и `gpt-6-luna`: Responses API, JSON-формат, reasoning, цены за вход, кэш и выход. У меня нет доступа к сайту OpenAI из среды.

Если что-то в журнале непонятно или неверно, напиши это прямо в журнал, в «Открытые вопросы». Я отвечу там же.
